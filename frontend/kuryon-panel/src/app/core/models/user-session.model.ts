export type { UserRole, AuthSession as UserSession } from './auth-contract.types';

/** Demo mağaza anahtarı — OrderStateService.storeId ile eşleşir. */
export const SESSION_STORE_ID_KANATCI = 'kanatci-bahattin';
